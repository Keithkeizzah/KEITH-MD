"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTypeErrorMessage = exports.getURL = exports.getQueryString = void 0;
const constants_1 = require("../constants");
const getQueryString = (data) => {
    const result = {};
    for (const key of Object.keys(data)) {
        const value = data[key];
        if (value) {
            if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
                result[key] =
                    typeof value === 'number' ? value.toString() : typeof value === 'boolean' ? `${value}` : value;
            else
                result[key] = value.join(',');
        }
    }
    if (!Object.keys(result).length)
        return '';
    const query = new URLSearchParams(result);
    return `?${query.toString()}`;
};
exports.getQueryString = getQueryString;
const getURL = (method, ...routes) => constants_1.API_URL.concat(`/${method}`, ...routes.map((route) => `/${route}`));
exports.getURL = getURL;
const getTypeErrorMessage = (parameter, expectedType, got) => `Expected type ${expectedType} for the parameter '${parameter}', but received type ${got}.`;
exports.getTypeErrorMessage = getTypeErrorMessage;
